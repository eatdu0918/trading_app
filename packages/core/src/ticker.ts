export interface Ticker24h {
  symbol: string;
  openPrice: number;
  highPrice: number;
  lowPrice: number;
  lastPrice: number;
  volume: number;
  priceChange: number;
  priceChangePercent: number;
}
